<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FertCalculatorMaui"
             xmlns:viewmodels="clr-namespace:FertCalculatorMaui.ViewModels"
             x:Class="FertCalculatorMaui.SaveMixPage"
             x:DataType="viewmodels:SaveMixViewModel"
             Title="Save Mix">
    
    <VerticalStackLayout Spacing="15" Padding="20" VerticalOptions="Center">
        <Label Text="Save Fertilizer Mix" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
        
        <Label Text="Mix Name" />
        <Entry x:Name="MixNameEntry" Placeholder="Enter a name for this mix" Text="{Binding MixName}" />
        
        <Label Text="Existing Mixes" IsVisible="{Binding HasExistingMixes}" />
        <Picker x:Name="ExistingMixesPicker" 
                ItemsSource="{Binding ExistingMixNames}" 
                SelectedItem="{Binding SelectedMixName}"
                IsVisible="{Binding HasExistingMixes}" />
        
        <Label Text="Overwrite selected mix?" IsVisible="{Binding IsOverwriteVisible}" />
        <CheckBox x:Name="OverwriteCheckbox" 
                 IsChecked="{Binding OverwriteExisting}"
                 IsVisible="{Binding IsOverwriteVisible}" />
        
        <!-- Error message display -->
        <Label Text="{Binding ErrorMessage}" TextColor="Red" IsVisible="{Binding HasError}" />
        
        <HorizontalStackLayout HorizontalOptions="Center" Spacing="20" Margin="0,20,0,0">
            <Button Text="Save" Clicked="OnSaveClicked" WidthRequest="100" />
            <Button Text="Cancel" Clicked="OnCancelClicked" WidthRequest="100" />
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>
